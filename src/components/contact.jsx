import React from "react";
import ContactContainer from "./Contactcomponents/ContactContainer.jsx";

function Contact() {

    return ( 

        
        < div className = "contact" > 

            < br / > < br / > < br / > < br / > < br / > < br / > 

            < ContactContainer / >

        </div >

     );


    }
export default Contact;